#pragma once

#include <proto/exec.h>
#include <hardware/custom.h>

struct vec2w {
    SHORT x, y;
};

struct vec4w {
    SHORT x, y, z, w;
};

struct vec2x {
    LONG x, y;
};

struct vec4x {
    LONG x, y, z, w;
};

